name: test a

on: workflow_call
# awddwa...
jobs:
  build:
    name: integration
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v3
        with:
          name: homework
      - run: |
          test=$(grep -e "flask_image=" test.txt | sed "s/flask_image=//g")
          echo $test